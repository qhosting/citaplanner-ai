# MIRROR DOCKERFILE - CITAPLANNER (EASYPANEL READY)
# FECHA DE AUDITORÍA: 2025

CONFIGURACIÓN DETECTADA:
------------------------
1. Base: node:20-alpine
2. Estrategia: Multi-stage (Reduce tamaño de imagen de 1GB a ~150MB)
3. Prisma: Ejecuta 'prisma generate' en fase de build.
4. Seguridad: No corre como Root (usa usuario nextjs).
5. Output: standalone (optimizado para microservicios).

NOTAS DE EASYPANEL:
------------------
Easypanel leerá este Dockerfile automáticamente al detectar el archivo en la raíz.
Asegúrese de mapear el puerto 3000 en la configuración del servicio.
