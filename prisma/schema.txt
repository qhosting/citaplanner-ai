// COPIA DE SEGURIDAD - SCHEMA PRISMA CITAPLANNER (MIRROR)
// ECOSISTEMA: AURUM CAPITAL TECHNOLOGY

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Estructura Multitenant detectada:
// - Business: Contenedor principal de marca y configuración.
// - User/Client/Service: Entidades vinculadas por businessId.
// - Appointment: Nodo de unión con validación de estados.
